package rtg;

import java.io.BufferedReader;
import java.io.FileNotFoundException;
import java.io.FileReader;
import java.util.Map;
import java.util.TreeMap;

import org.ansj.library.UserDefineLibrary;

public class GraphViewer {

	public static String pgrPath = "";

	public static String lpaPath = "";

	public static void main(String[] args) throws FileNotFoundException {
		Map<String, Double[]> map = new TreeMap<String, Double[]>();
		String pgrLine, lpaLine;
		BufferedReader pgrBr = new BufferedReader(new FileReader(pgrPath));
		BufferedReader lpaBr = new BufferedReader(new FileReader(lpaPath));
		while ((line = br.readLine()) != null) {
			UserDefineLibrary.insertWord(line, "nameDic", 1000);
		}
	}
}
